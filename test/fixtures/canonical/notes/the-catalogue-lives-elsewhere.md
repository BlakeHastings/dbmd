---
kind: note
layout: { x: 480, y: 800, w: 320, h: 200 }
color: slate
---

`products` is a read-only copy. The catalogue service is the owner and it is
reached over HTTP, so a column added there does not appear here until somebody
adds it to the nightly sync.
