---
kind: table
table: misfiled
columns:
  - name: id
    type: uuid
    pk: true
---

Says it is a table and sits in `notes/`. The directory decides, so this is a
diagnostic and not a note with a lot of surprising keys on it.
